
无法对选中的一段文字就行连续加粗、下划线和斜体操作 ！！！

IE6中测试，撤销不顺畅，有时候点击两次才有效

全屏（初始化div宽度）

IE6、7插入图片有问题，能插入，但是报错误

indent（基本命令）

插入视频 （弹出框方式）

h1-h6 标题 （下拉框方式）

插入代码（弹出框方式）

上传图片（弹出框方式）

颜色块

撤销时，也要撤销selection range

插入链接 的callback不执行

处理那些硬编码的数字，例如 height = height - $btnContainer.height() - $statusDiv.height() - 11;

（完整版、mini版、移动版）