/**
 * @description 校验用户插入的链接是否合法
 * @author maxinwei
 */

export default {
    linkCheck: function (text: string, link: string): any {
        console.log(text)
        console.log(link)
        return true
    },
}
